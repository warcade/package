/**
 * WebArcade Platform Version
 * Single source of truth for version information
 */

export const VERSION = '0.1.5';
export const VERSION_NAME = 'WebArcade';
export const VERSION_FULL = `${VERSION_NAME} v${VERSION}`;

export default {
    version: VERSION,
    name: VERSION_NAME,
    full: VERSION_FULL
};
